#include <string>
using namespace std;

class Solution {
public:
  void reverseWords(string &s) {
    if (s.empty())
      return;
  }

  //private:
  //  // Remove trailing/leading/multiple spaces
  //  void trim(string& s) {
  //    for (int i = -1, j = 0; j < s.length(); ++j) {
  //      
  //    }
  //  }
};