#include <cstdlib>
#include <vector>
using namespace std;

#include "binary_tree_upside_down.h"
#include "../util/util.h"

TreeNode* BinaryTreeUpsideDown::upsideDownBinaryTree(TreeNode* root) {
  if (root == NULL)
    return NULL;


}
